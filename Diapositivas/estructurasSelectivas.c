#include <stdio.h>
#include <stdlib.h>

int main() {

    /* Estructura if:
        if (CondControl) {
            accionesSI
        }
        else if (CondControl1) {
            accionesCC1
        }
        else if (CondControl2) {
            accionesCC2
        }
        else {
            accioneselse
        }

        accionseguida


      Estructuras switch
        switch (expresion) {
            case exp-const: acciones
                            break;
            case exp-const: acciones
                            break;
            case exp_const: acciones
                            break;
            default       : acciones
                            break;
        }
        
      Estructuras Repetitivas

        while(expresion) {
            acciones
        }
        for(expr1;expr2;expr3){
            acciones
        }
        do {
            acciones
        } while (expresion);
    */
}