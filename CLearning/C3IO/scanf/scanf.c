#include <stdio.h>

int main(){

    int i, j;
    float x, y;
    printf("Introduce los números\n");
    scanf("%d%d%f%f", &i, &j, &x, &y);
    printf("i = %d, j = %d, x = %f, y = %f", i, j, x, y);

}